{
	"name": "openai-compatible-assistant",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "echo 'Build completed - using wrangler for TypeScript compilation'",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.32.0"
	},
	"dependencies": {
		"crypto-js": "^4.2.0",
		"dompurify": "^3.1.7",
		"helmet": "^7.1.0",
		"openai": "^5.15.0",
		"zod": "^4.1.1"
	}
}
